# SDC25
SDC 2024-2025 CRANSEDS QR Code detection Script

## Detect a QR Code: get its location, inclination, distance, angular velocity ....
For now it detects a qrcode, puts a blue box around it and displays the location

import pyyaml
import opencv-python
import pyzbar
